<!-- app/views/groups/show.html.erb -->
<div class="container">
  <h1 class="text-center mt-5">Liste de vos groupes</h1>
  <div class="btn-div">
    <%= link_to "Créer un groupe 🐶", new_profile_group_path(@profile), class: "button-form btn btn-link", style: "text-decoration:none;" %>
  </div>

  <% if @groups.any? %>
    <div class="card-container">
      <% @groups.each do |group| %>
        <%= link_to profile_group_path(@profile, group), class: "card-link" do %>
          <div class="card-product">
            <% if group.photo.present? %>
              <%= cl_image_tag group.photo.key, height: 300, width: 400, crop: :fill %>
            <% else %>
              <%= image_tag("https://res.cloudinary.com/drp9liavk/image/upload/v1719308669/avatar2_byf4no.png", height: 300, width: 400, crop: :fill)%>
            <% end %>
            <div class="card-product-infos">
              <h2><%= group.name %></h2>
              <p><%= group.description %></p>
            </div>
          </div>
        <% end %>
      <% end %>
    </div>
  <% else %>
    <p>Vous n'appartenez à aucun groupe pour le moment.</p>
  <% end %>
  <%= link_to "Revenir a l'accueil", root_path, class: "text-decoration-none" %>
</div>
